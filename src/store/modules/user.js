// initial state
const state = {
}

// mutations
const mutations = {
  UPDATE_HASWATCHMESSAGE (state, status) {
    state.hasWatchMessage = status
  }
}

export default {
  // getters,
  // actions,
  state,
  mutations
}
